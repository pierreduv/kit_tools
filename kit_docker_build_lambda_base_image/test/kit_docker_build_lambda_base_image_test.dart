import 'package:kit_docker_build_lambda_base_image/kit_docker_build_lambda_base_image.dart';
import 'package:test/test.dart';

void main() {
  test('calculate', () {
    expect(calculate(), 42);
  });
}
